import { PosWorkspace } from "../components/PosWorkspace";

export default function HomePage() {
  return (
    <main className="main-content app-shell px-3 sm:px-5 md:px-6">
      <PosWorkspace />
    </main>
  );
}
